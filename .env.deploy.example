# Configuración para el despliegue a ECR
# Copia este archivo a .env.deploy y modifica los valores según tu entorno

# Configuración AWS
AWS_REGION=us-east-2
REPOSITORY_NAME=fast-api-app
IMAGE_TAG=latest
PLATFORM=linux/amd64

# Configuración adicional (opcional)
# Si quieres usar un perfil específico de AWS CLI
# AWS_PROFILE=default

# Si quieres usar una cuenta específica (normalmente se detecta automáticamente)
# AWS_ACCOUNT_ID=123456789012
